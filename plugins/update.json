{
  "plugins": [
    {
      "id":"fokkeensukke",
      "stripSource":"https://www.nrc.nl/fokke-sukke",
      "extractJson":"function(page) {var regex = /.*/; var match = regex.exec(page); return match[1]; }"
    }
  ]
}
